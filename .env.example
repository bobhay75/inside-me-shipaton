# Public RevenueCat Android SDK key from your RevenueCat project.
EXPO_PUBLIC_REVENUECAT_API_KEY=

# Optional HTTPS endpoint that accepts {text,mood} and returns {reflection}.
# Keep OpenAI/Gemini secret keys on the server, never in this app.
EXPO_PUBLIC_REFLECTION_API_URL=
